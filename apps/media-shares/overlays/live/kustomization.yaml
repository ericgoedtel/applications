---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base/

patches:
  - target:
      kind: PersistentVolume
      name: nfs-postgresql
    patch: |
      - op: replace
        path: /spec/nfs/path
        value: /srv/shares/databases/live
